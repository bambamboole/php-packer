# Contributing

Contributions are welcome.

Before opening a pull request, install the dependencies and run the complete local gate:

```bash
composer test
composer test:lint
composer analyse
```

Changes in behavior must include tests and corresponding README updates. Keep each pull request focused on one change and use meaningful commits.
